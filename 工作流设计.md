# 工作流设计 — AI简历优化Agent（Coze平台）

> 以下工作流基于Coze（扣子）平台搭建，使用其可视化工作流编辑器。整体搭建预计耗时2-3小时。

---

## 一、工作流概览

```
┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌─────────┐
│  开始   │────▶│ JD解析   │────▶│ 匹配分析 │────▶│ 简历优化 │────▶│ 格式输出 │────▶│  结束   │
│ (输入)  │     │ (LLM-1)  │     │ (LLM-3)  │     │ (LLM-2)  │     │ (代码)   │     │ (输出)  │
│         │     └──────────┘     ▲          │     └──────────┘     └─────────┘     └─────────┘
│         │     ┌──────────┐     │
│         │────▶│ 简历解析 │─────┘
│         │     │ (LLM-2)  │
└─────────┘     └──────────┘
```

**执行逻辑**：
- 节点1（JD解析）与节点2（简历解析）**并行执行**，提升响应速度
- 节点3（匹配分析）等待节点1、2均完成后执行
- 节点4（简历优化）在节点3完成后执行
- 节点5（格式输出）做最终渲染

---

## 二、节点详细配置

### 节点0：开始节点（Start）

| 配置项 | 值 |
|--------|-----|
| 类型 | 开始节点 |
| 输入变量1 | `jd_text`，类型：String，描述：目标岗位的JD文本 |
| 输入变量2 | `resume_text`，类型：String，描述：用户的简历文本 |

**要点**：两个输入变量均设为必填，在开场白中引导用户按格式输入。

---

### 节点1：JD智能解析（LLM节点）

| 配置项 | 值 |
|--------|-----|
| 类型 | LLM节点 |
| 模型 | DeepSeek-V3（或通义千问Qwen-Max） |
| System Prompt | 对应「核心Prompt设计.md」中的 **Prompt 1** |
| 输入变量 | `jd_text` ← 引用开始节点的 `jd_text` |
| 输出变量 | `jd_parsed`（LLM输出，JSON字符串） |
| Temperature | 0.1（解析类任务需要低随机性） |
| Max Tokens | 2000 |

**配置要点**：
- 开启JSON模式输出，确保结构化结果稳定
- 添加失败重试逻辑（最多2次）
- 输出格式校验：确认是合法JSON后传递给下游

---

### 节点2：简历结构化解析（LLM节点）

| 配置项 | 值 |
|--------|-----|
| 类型 | LLM节点 |
| 模型 | DeepSeek-V3 |
| System Prompt | 对应「核心Prompt设计.md」中的 **Prompt 2** |
| 输入变量 | `resume_text` ← 引用开始节点的 `resume_text` |
| 输出变量 | `resume_parsed`（LLM输出，JSON字符串） |
| Temperature | 0.1 |
| Max Tokens | 3000 |

**配置要点**：
- 与节点1并行执行，通过分支汇聚等待两者完成
- 简历文本可能较长，确认模型的context window足够（DeepSeek-V3支持128K）

---

### 节点3：匹配分析（LLM节点）

| 配置项 | 值 |
|--------|-----|
| 类型 | LLM节点 |
| 模型 | DeepSeek-V3 |
| System Prompt | 见下方 |
| 输入变量 | `jd_parsed` ← 节点1输出 / `resume_parsed` ← 节点2输出 |
| 输出变量 | `match_report`（包含评分与关键词覆盖） |
| Temperature | 0.2 |
| Max Tokens | 2000 |

**节点3的System Prompt**：
```
你是简历匹配分析专家。根据JD解析结果和简历解析结果，分析两者的匹配度。
输出格式：
{
  "总分": 0-100,
  "维度评分": {"技能匹配": 0-30, "经验匹配": 0-30, "关键词覆盖": 0-20, "行业匹配": 0-10, "简历质量": 0-10},
  "已覆盖关键词": ["keyword1", "..."],
  "未覆盖关键词": ["keyword2", "..."],
  "匹配分析摘要": "string"
}

JD解析结果：{{jd_parsed}}
简历解析结果：{{resume_parsed}}
```

---

### 节点4：简历优化生成（LLM节点）

| 配置项 | 值 |
|--------|-----|
| 类型 | LLM节点 |
| 模型 | DeepSeek-V3 |
| System Prompt | 对应「核心Prompt设计.md」中的 **Prompt 3** |
| 输入变量 | `jd_parsed` ← 节点1输出 / `resume_parsed` ← 节点2输出 / `match_report` ← 节点3输出 |
| 输出变量 | `optimized_resume`（最终优化结果） |
| Temperature | 0.4（生成类任务可适当提高创造性） |
| Max Tokens | 4000 |

**配置要点**：
- 这是核心输出节点，Token上限设高
- 输出为Markdown格式，包含评分、优化简历、建议三部分

---

### 节点5：格式输出（结束节点）

| 配置项 | 值 |
|--------|-----|
| 类型 | 结束节点 |
| 输出变量 | `optimized_resume` ← 引用节点4输出 |
| 输出格式 | Markdown |

---

## 三、变量传递总览

| 变量名 | 类型 | 产生节点 | 消费节点 |
|--------|------|----------|----------|
| `jd_text` | String | 开始节点 | 节点1 |
| `resume_text` | String | 开始节点 | 节点2 |
| `jd_parsed` | JSON String | 节点1 | 节点3、节点4 |
| `resume_parsed` | JSON String | 节点2 | 节点3、节点4 |
| `match_report` | JSON String | 节点3 | 节点4 |
| `optimized_resume` | String | 节点4 | 结束节点 |

---

## 四、搭建步骤与时间估算

| 步骤 | 操作 | 预计耗时 |
|------|------|----------|
| 1 | 创建Coze应用，配置开场白与输入引导 | 15分钟 |
| 2 | 搭建工作流：添加6个节点并连线 | 20分钟 |
| 3 | 配置节点1（JD解析Prompt）+ 调试 | 25分钟 |
| 4 | 配置节点2（简历解析Prompt）+ 调试 | 25分钟 |
| 5 | 配置节点3（匹配分析Prompt）+ 调试 | 20分钟 |
| 6 | 配置节点4（简历优化Prompt）+ 调试 | 30分钟 |
| 7 | 端到端测试（3轮测试用例） | 30分钟 |
| 8 | 优化输出格式 + 发布 | 15分钟 |
| **合计** | | **约3小时** |

---

## 五、调试建议

1. **逐步调试**：先单独测试节点1和节点2，确保JSON输出稳定后再串联
2. **边界测试**：准备3组测试用例——标准JD+标准简历、极短JD（50字）、格式混乱的简历
3. **Prompt迭代**：如果某个节点输出不稳定，优先调整few-shot示例而非修改规则描述
4. **Token监控**：关注每个节点的实际Token消耗，节点4（简历优化）通常是消耗最大的，必要时精简输入

---

## 六、扩展方向

- **多轮对话**：增加"用户确认"环节，让用户选择是否接受某条优化建议
- **PDF导出**：在结束节点后接入文档生成插件，输出排版好的PDF简历
- **批量优化**：支持一次输入多个JD，生成多份针对性简历
- **简历模板库**：预置多种行业/岗位的简历模板，优化时套用对应模板

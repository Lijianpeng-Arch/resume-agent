# 项目A：AI简历优化Agent

## 这是什么

一个能自动分析JD和简历匹配度、并给出优化建议的工具。输入岗位JD + 你的简历，它告诉你匹配度多少分、哪些关键词没覆盖、简历哪里需要改。

## 怎么使用

### 方式一：本地网站版（推荐，有界面、可接真AI）

`webapp/` 文件夹是一个完整的本地网站，双击就能用：

**Windows**：双击 `webapp/启动网站.bat`（会自动打开浏览器）
**Mac/Linux**：`cd webapp && chmod +x start.sh && ./start.sh`，然后浏览器打开 http://127.0.0.1:8001

**两种运行模式**：
- **不配 Key（离线模式）**：打开就能用，内置规则引擎完成匹配度分析，无需联网
- **配置 API Key（AI在线模式）**：点右上角 ⚙️ 设置 → 粘贴你的 LLM API Key → 点「自动识别并保存」（自动识别是 DeepSeek/Kimi/智谱/通义/MiniMax/OpenAI 哪家，并拉出可用模型列表）→ 点「测试连接」确认可用 → 之后所有分析都由真 AI 完成

> 支持任何 OpenAI 兼容接口；智谱的 Key 中间含 `.` 会被优先识别。Key 只保存在你自己电脑的 `webapp/config.json` 里，不上传任何地方。

### 方式二：命令行脚本（最轻量）

#### 前提条件
- 电脑上装了 Python 3.6+（Mac/Linux自带，Windows去 python.org 下载）
- 不需要安装任何第三方库，不需要联网

#### 快速开始（3步）

```bash
# 第1步：进入项目目录
cd code/

# 第2步：运行脚本（使用内置的示例数据）
python3 resume_agent.py

# 第3步：查看输出结果
# 终端会直接打印完整的分析报告，包括：
# - JD关键词提取结果
# - 简历解析结果
# - 匹配度评分（5个维度）
# - 缺失技能清单
# - 优化建议
# - 优化后的简历模板
```

### 用自己的JD和简历

打开 `resume_agent.py`，找到文件末尾的 `if __name__ == "__main__":` 部分，修改这两个变量：

```python
# 改成你的目标岗位JD
jd_text = """
【岗位名称】你想投的岗位名称
【岗位职责】
1. ...
2. ...

【任职要求】
1. ...
2. ...
"""

# 改成你的简历内容
resume_text = """
你的姓名 | 职位 | X年经验
电话：xxx | 邮箱：xxx

【工作经历】
...
"""
```

保存后重新运行 `python3 resume_agent.py` 即可。

## 文件说明

| 文件 | 说明 |
|------|------|
| `resume_agent.py` | 主程序（单文件，约960行） |
| `test_output.log` | 实际运行日志（证明脚本已跑通） |
| `AI简历优化Agent-交互演示.html` | 网页版演示（双击浏览器打开） |
| `核心Prompt设计.md` | 3套Prompt设计（可直接用于Coze/Dify） |
| `工作流设计.md` | Coze工作流配置指南 |
| `项目设计方案.md` | 项目背景、需求分析、技术架构 |
| `演示案例.md` | 端到端演示案例 |

## 技术架构

```
JD解析器(JDParser)
  ↓ 提取技能关键词、经验年限、学历要求
简历解析器(ResumeParser)
  ↓ 提取技能、教育背景、工作年限
匹配引擎(MatchEngine)
  ↓ 5维度评分（技能40分+经验25分+学历15分+项目10分+相关度10分）
优化器(ResumeOptimizer)
  ↓ 生成具体建议 + 优化后简历模板
报告生成器(ReportGenerator)
  ↓ 输出结构化JSON报告
```

## 常见问题

**Q: 需要联网吗？**
A: 不需要。纯本地运行，不调用任何API。

**Q: 需要安装什么吗？**
A: 只需要Python 3.6+。不依赖任何第三方库。

**Q: Windows能用吗？**
A: 能。Windows装了Python后，用 `python resume_agent.py` 运行即可。

**Q: 能接入真正的AI吗？**
A: 可以。代码中的Prompt设计已预留接口，将 `resume_agent.py` 中的模板引擎替换为LLM API调用即可。核心Prompt设计文件中有3套可直接用于Coze/Dify的完整Prompt。

# AI简历优化Agent — 产品设计方案

## 一、项目背景与痛点分析

### 1.1 市场现状

2024年中国高校毕业生人数达1179万，叠加海归求职群体与社会流动人员，求职市场竞争异常激烈。据猎聘《2024求职行为调研报告》，求职者平均每投递1个岗位需要花30-45分钟修改简历，而一个活跃求职者平均每周投递15-20个岗位，意味着每周仅简历修改就耗费8-15小时。

### 1.2 核心痛点

| 痛点 | 数据支撑 | 用户原声 |
|------|----------|----------|
| JD理解成本高 | 68%的求职者表示"不确定自己经历是否符合JD要求" | "JD写了20条要求，我到底满足几条？" |
| 关键词不匹配 | ATS系统会过滤掉不含JD关键词的简历，淘汰率高达75% | "投了200份简历，面试不到5个" |
| 成就描述模糊 | 82%的简历使用"负责""参与"等模糊动词，缺乏量化数据 | "我知道自己做得不错，但不知道怎么写出亮点" |
| 一稿多投低效 | 73%的求职者用同一份简历投所有岗位，命中率极低 | "每次改简历都头疼，干脆不改了" |

### 1.3 机会判断

大语言模型（LLM）在语义理解、文本改写、结构化提取方面的能力已经成熟。通过Agent化封装，可以将"JD解析→简历分析→匹配优化"这一复杂流程自动化，将原本30分钟的手动工作压缩至2分钟内完成。

---

## 二、产品定位与目标用户

**一句话定位**：输入JD和简历，2分钟输出针对该岗位定制优化的高匹配度简历。

### 目标用户画像

| 维度 | 核心用户 | 次要用户 |
|------|----------|----------|
| 身份 | 1-3年经验的求职者 | 应届毕业生 |
| 年龄 | 24-30岁 | 21-24岁 |
| 场景 | 频繁跳槽、跨行业求职 | 初次求职、海投阶段 |
| 付费意愿 | 中高（愿为效率付费） | 中低（价格敏感） |
| 核心诉求 | "帮我精准匹配目标岗位" | "帮我写出看起来专业的简历" |

---

## 三、核心功能

### 3.1 JD智能解析

自动提取岗位JD中的结构化信息：岗位名称、所属部门、核心职责、硬性要求（学历/经验/技能）、加分项、薪资范围、工作地点。输出JSON格式，便于下游节点消费。

### 3.2 简历亮点提取

从用户原始简历中识别关键信息：个人信息、教育背景、工作经历（含公司/职位/时间/职责）、项目经验、技能清单。重点提取可量化的成就指标（如"提升转化率30%""管理5人团队"）。

### 3.3 关键词匹配评分

将JD关键词与简历关键词进行语义匹配（非简单字符串匹配，而是基于向量相似度的语义匹配）。输出匹配度评分（0-100分）及关键词覆盖明细，帮助用户快速定位差距。

### 3.4 量化改写建议

针对简历中模糊的描述，提供具体的量化改写方案。例如将"负责产品运营工作"改写为"主导核心产品运营，DAU从5万提升至12万（+140%），用户留存率提高至78%"。同时确保改写内容真实可信，不虚构数据。

---

## 四、技术架构

```
用户输入
  ├── JD文本（粘贴或URL）
  └── 简历文本（粘贴或上传PDF/Word）
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  工作流引擎（Coze Workflow / Dify Workflow）                  │
│                                                              │
│  节点1：JD解析节点                                           │
│    └─ 调用 LLM → 输出结构化JD（JSON）                        │
│                                                              │
│  节点2：简历解析节点（与节点1并行执行）                         │
│    └─ 调用 LLM → 输出结构化简历（JSON）                       │
│                                                              │
│  节点3：匹配分析节点                                         │
│    ├─ 输入：结构化JD + 结构化简历                             │
│    ├─ 处理：关键词提取→语义匹配→评分计算                      │
│    └─ 输出：匹配度评分 + 关键词覆盖报告                       │
│                                                              │
│  节点4：简历优化生成节点                                      │
│    ├─ 输入：结构化JD + 结构化简历 + 匹配分析报告              │
│    ├─ 处理：关键词植入 + 成就量化 + 经历重排                  │
│    └─ 输出：优化后简历 + 3条改进建议                          │
│                                                              │
│  节点5：格式化输出节点                                        │
│    └─ Markdown渲染 → 最终交付                                 │
└─────────────────────────────────────────────────────────────┘
       │
       ▼
用户输出
  ├── 匹配度评分卡片
  ├── 优化后的完整简历
  └── 3条针对性改进建议
```

---

## 五、工具栈选择

| 组件 | 选择 | 理由 |
|------|------|------|
| 工作流平台 | Coze（扣子） | 原生支持多节点编排、变量传递、LLM调用，可视化调试效率高 |
| 主力LLM | DeepSeek-V3 | 中文理解能力强、API成本低（0.1元/万token）、结构化输出稳定 |
| 备选LLM | 通义千问Qwen-Max | 阿里系兼容性好，简历解析场景表现优秀，支持长上下文 |
| 文档解析 | 内置PDF/Word解析插件 | 支持用户上传原始文档，降低使用门槛 |
| 输出格式 | Markdown + PDF导出 | 兼顾可读性与可交付性 |

---

## 六、量化效果（合理估算）

| 指标 | 优化前（手动） | 优化后（Agent） | 提升幅度 |
|------|----------------|-----------------|----------|
| 单份简历优化耗时 | 30-45分钟 | 2-3分钟 | 效率提升 **90%+** |
| JD关键词覆盖率 | 30-40%（凭感觉） | 75-85%（系统化匹配） | 覆盖率提升 **2倍+** |
| ATS通过率 | ~25% | ~55%（估算） | 通过率提升 **120%+** |
| 量化描述占比 | <20% | >80% | 量化程度提升 **4倍** |
| 每周节省时间 | — | 6-12小时/人 | — |

> 注：以上数据为基于产品设计与行业经验的合理估算，实际效果需通过用户测试验证。
